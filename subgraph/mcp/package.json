{
  "name": "agent-grid-mcp",
  "version": "0.1.1",
  "description": "MCP server: any agent queries live AgentGrid protocol state via The Graph and builds signable payloads. *_tx tools return unsigned {to, data, value} for an external signer; sign_and_send broadcasts using a role key from the server env (keys never appear in output), so agents never touch keys or run cast.",
  "type": "module",
  "bin": {
    "agent-grid-mcp": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "viem": "^2.31.0"
  }
}
